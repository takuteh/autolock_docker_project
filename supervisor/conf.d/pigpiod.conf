[program:pigpiod]
    command=pigpiod
    autostart=true
    autorestart=true
    startretries=3
    user=root
    stdout_logfile=/var/log/supervisor/pigpiod.log
    stderr_logfile=/var/log/supervisor/pigpiod_err.log
    priority=1  # 起動優先度